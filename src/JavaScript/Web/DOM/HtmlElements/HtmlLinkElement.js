export const href = (image) => () => image.href

export const setHref = (value) => (image) => () => image.href = value

export const rel = (image) => () => image.rel

export const setRel = (value) => (image) => () => image.rel = value

export const type_ = (image) => () => image.type

export const setType = (value) => (image) => () => image.type = value
